apiVersion: v1
kind: Service
metadata:
    name: "dropaks-3fa0"
    labels:
        app: "dropaks-3fa0"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "dropaks-3fa0"