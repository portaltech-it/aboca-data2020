apiVersion: v1
kind: Service
metadata:
    name: "dropaks-2a90"
    labels:
        app: "dropaks-2a90"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "dropaks-2a90"